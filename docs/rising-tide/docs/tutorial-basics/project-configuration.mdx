import ModulesFlakeProject from "../_generated/modules.flake.project.md";

# Project configuration

The argument to `risingTideLib.mkProject` takes the form of a nix module with options documented below.

<ModulesFlakeProject />
